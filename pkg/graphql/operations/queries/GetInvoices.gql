# Fetch paginated list of invoices with line items and payment status
query GetInvoices($first: Int = 50, $after: String) {
  invoicesConnection(page: { first: $first, after: $after }) @paginate {
    edges {
      cursor
      node {
        id
        amount
        prepaidCreditApplied
        currency
        status
        dueDate
        createdAt
        lineItems {
          lineKey
          meter
          description
          quantity
          includedQuantity
          billableQuantity
          unitPrice
          total
          currency
          clusterId
          clusterName
          clusterKind
          pricingSource
          pricingLabel
        }
      }
    }
    pageInfo {
      hasNextPage
      hasPreviousPage
      startCursor
      endCursor
    }
    totalCount
  }
}
