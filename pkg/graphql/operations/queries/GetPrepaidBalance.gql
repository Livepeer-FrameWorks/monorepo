query GetPrepaidBalance($currency: String = "EUR") {
  prepaidBalance(currency: $currency) {
    id
    tenantId
    balanceCents
    currency
    lowBalanceThresholdCents
    isLowBalance
    createdAt
    updatedAt
  }
}
