# FrameWorks Web Application - Environment Variables

# Auth API (local non-Docker)
VITE_AUTH_URL=http://localhost:18090/auth

# GraphQL (local non-Docker)
VITE_GRAPHQL_HTTP_URL=http://localhost:18090/graphql/
VITE_GRAPHQL_WS_URL=ws://localhost:18090/graphql/ws

# Marketing Website URL (local dev server)
VITE_MARKETING_SITE_URL=http://localhost:18090

# Docs Website URL (local dev server)
VITE_DOCS_SITE_URL=http://localhost:18090/docs

# Streaming Configuration - Base URLs (apps parse hostname and derive TLS mode from scheme)
# For production: use https:// URLs
VITE_STREAMING_INGEST_URL=http://localhost:8080
VITE_STREAMING_EDGE_URL=http://localhost:8080

# Protocol-specific ports (RTMP and SRT need explicit ports)
VITE_STREAMING_RTMP_PORT=1935
VITE_STREAMING_SRT_PORT=8889

# Streaming paths
VITE_STREAMING_RTMP_PATH=/live
VITE_STREAMING_HLS_PATH=/hls
VITE_STREAMING_WEBRTC_PATH=/webrtc
VITE_STREAMING_EMBED_PATH=/

# Human verification - Auth (Cloudflare Turnstile test key for local development)
VITE_TURNSTILE_AUTH_SITE_KEY=1x0000000000000000000000000000000AA

# Optional: override default dev server port/host when running `pnpm dev`
# PORT=3000
# HOST=0.0.0.0
