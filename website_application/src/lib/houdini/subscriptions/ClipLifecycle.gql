subscription ClipLifecycle($stream: String!) {
  clipLifecycle(stream: $stream) {
    stage
    clipHash
    requestId
    progressPercent
    filePath
    s3Url
    sizeBytes
    error
    startedAt
    completedAt
    nodeId
    internalName
  }
}
