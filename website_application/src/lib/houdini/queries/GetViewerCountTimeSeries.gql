query GetViewerCountTimeSeries(
  $stream: String
  $timeRange: TimeRangeInput
  $interval: String = "5m"
) {
  viewerCountTimeSeries(
    stream: $stream
    timeRange: $timeRange
    interval: $interval
  ) {
    timestamp
    viewerCount
    stream
  }
}
