# Fetch tenant usage summary with resource consumption and costs for billing period
query GetTenantUsage($timeRange: TimeRangeInput) {
  tenantUsage(timeRange: $timeRange) {
    billingPeriod
    usage {
      resourceType
      amount
    }
    costs {
      resourceType
      cost
    }
    totalCost
    currency
  }
}
