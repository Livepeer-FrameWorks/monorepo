# Validate stream key and return validation status with error details if invalid
query ValidateStreamKey($streamKey: String!) {
  validateStreamKey(streamKey: $streamKey) {
    status
    streamKey
    error
  }
}
