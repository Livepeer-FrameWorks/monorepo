# Fetch detailed invoice by ID including line items and payment status
query GetInvoice($id: ID!) {
  invoice(id: $id) {
    id
    amount
    currency
    status
    dueDate
    createdAt
    lineItems {
      description
      quantity
      unitPrice
      total
    }
  }
}
