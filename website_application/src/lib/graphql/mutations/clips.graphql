# Clip management mutations
mutation CreateClip($input: CreateClipInput!) {
  createClip(input: $input) {
    id
    stream
    title
    description
    startTime
    endTime
    duration
    playbackId
    status
    createdAt
    updatedAt
  }
}

mutation DeleteClip($id: ID!) {
  deleteClip(id: $id)
}
