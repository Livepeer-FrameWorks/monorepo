# Clip management mutations
mutation CreateClip($input: CreateClipInput!) {
  createClip(input: $input) {
    id
    streamId
    tenantId
    title
    description
    startTime
    endTime
    duration
    playbackId
    status
    createdAt
    updatedAt
  }
}