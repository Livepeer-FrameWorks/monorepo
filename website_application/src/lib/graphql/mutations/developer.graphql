# Developer API mutations
mutation CreateAPIToken($input: CreateDeveloperTokenInput!) {
  createDeveloperToken(input: $input) {
    id
    name
    token
    permissions
    expiresAt
    createdAt
  }
}

mutation RevokeAPIToken($id: ID!) {
  revokeDeveloperToken(id: $id)
}
