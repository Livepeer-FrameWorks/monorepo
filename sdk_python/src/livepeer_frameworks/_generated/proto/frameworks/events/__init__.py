# Generated by the protocol buffer compiler.  DO NOT EDIT!
# sources: events/options.proto
# plugin: python-betterproto2
# This file has been @generated

__all__ = ("EventSpec", "Scope", "Visibility")

from dataclasses import dataclass

import betterproto2

from ...message_pool import default_message_pool

_COMPILER_VERSION = "0.10.1"
betterproto2.check_compiler_version(_COMPILER_VERSION)


class Scope(betterproto2.Enum):
    """
    Scope says whether an event belongs to a tenant. TENANT events carry the
    owning tenant in the envelope; PLATFORM events never carry one.
    """

    UNSPECIFIED = 0

    TENANT = 1

    PLATFORM = 2

    @classmethod
    def betterproto_value_to_renamed_proto_names(cls) -> dict[int, str]:
        return {
            0: "SCOPE_UNSPECIFIED",
            1: "SCOPE_TENANT",
            2: "SCOPE_PLATFORM",
        }

    @classmethod
    def betterproto_renamed_proto_names_to_value(cls) -> dict[str, int]:
        return {
            "SCOPE_UNSPECIFIED": 0,
            "SCOPE_TENANT": 1,
            "SCOPE_PLATFORM": 2,
        }


class Visibility(betterproto2.Enum):
    """
    Visibility says who may receive an event type. PUBLIC types are the tenant
    contract (webhooks and GraphQL subscriptions serialize them unchanged);
    INTERNAL types are only read by platform services.
    """

    UNSPECIFIED = 0

    PUBLIC = 1

    INTERNAL = 2

    @classmethod
    def betterproto_value_to_renamed_proto_names(cls) -> dict[int, str]:
        return {
            0: "VISIBILITY_UNSPECIFIED",
            1: "VISIBILITY_PUBLIC",
            2: "VISIBILITY_INTERNAL",
        }

    @classmethod
    def betterproto_renamed_proto_names_to_value(cls) -> dict[str, int]:
        return {
            "VISIBILITY_UNSPECIFIED": 0,
            "VISIBILITY_PUBLIC": 1,
            "VISIBILITY_INTERNAL": 2,
        }


@dataclass(eq=False, repr=False)
class EventSpec(betterproto2.Message):
    """
    EventSpec is attached to every domain event message. It is the single source
    for the event's type string, audience, tenant scope, and the aggregate whose
    ID keys the Kafka record.
    """

    type: "str" = betterproto2.field(1, betterproto2.TYPE_STRING)
    """
    type is the CloudEvents ce_type and the public webhook type, for example
    "stream.created". It is unique across the registry.
    """

    visibility: "Visibility" = betterproto2.field(
        2, betterproto2.TYPE_ENUM, default_factory=lambda: Visibility(0)
    )

    scope: "Scope" = betterproto2.field(
        3, betterproto2.TYPE_ENUM, default_factory=lambda: Scope(0)
    )

    aggregate: "str" = betterproto2.field(4, betterproto2.TYPE_STRING)
    """
    aggregate is the plural resource name that prefixes the record key, for
    example "streams" in "streams/<id>".
    """


default_message_pool.register_message("frameworks.events", "EventSpec", EventSpec)
