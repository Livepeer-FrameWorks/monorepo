<?xml version="1.0"?>
<clickhouse>
    <!-- Network -->
    <http_port>8123</http_port>
    <tcp_port>9000</tcp_port>
    <interserver_http_port>9009</interserver_http_port>
    
    <!-- Listen on all interfaces in production -->
    <listen_host>::</listen_host>
    <listen_host>0.0.0.0</listen_host>
    
    <!-- Performance -->
    <max_connections>500</max_connections>
    <keep_alive_timeout>5</keep_alive_timeout>
    <max_concurrent_queries>100</max_concurrent_queries>
    <max_server_memory_usage>0</max_server_memory_usage> <!-- Use all available RAM -->
    
    <!-- Storage -->
    <path>/var/lib/clickhouse/</path>
    <tmp_path>/var/lib/clickhouse/tmp/</tmp_path>
    <user_files_path>/var/lib/clickhouse/user_files/</user_files_path>
    <format_schema_path>/var/lib/clickhouse/format_schemas/</format_schema_path>
    
    <!-- Logging -->
    <logger>
        <level>information</level>
        <log>/var/log/clickhouse-server/clickhouse-server.log</log>
        <errorlog>/var/log/clickhouse-server/clickhouse-server.err.log</errorlog>
        <size>1000M</size>
        <count>10</count>
    </logger>
    
    <!-- Timezone -->
    <timezone>UTC</timezone>

    <!-- Postgres reach-through for operator tenant-activity analytics. Dev uses
         one shared `frameworks` database; provisioned clusters render these
         collections from the manifest. Credentials are dev-only. -->
    <named_collections>
        <quartermaster_pg>
            <host>postgres</host>
            <port>5432</port>
            <database>frameworks</database>
            <schema>quartermaster</schema>
            <user>frameworks_analytics_ro</user>
            <password>frameworks_analytics_dev</password>
        </quartermaster_pg>
        <commodore_pg>
            <host>postgres</host>
            <port>5432</port>
            <database>frameworks</database>
            <schema>commodore</schema>
            <user>frameworks_analytics_ro</user>
            <password>frameworks_analytics_dev</password>
        </commodore_pg>
        <purser_pg>
            <host>postgres</host>
            <port>5432</port>
            <database>frameworks</database>
            <schema>purser</schema>
            <user>frameworks_analytics_ro</user>
            <password>frameworks_analytics_dev</password>
        </purser_pg>
    </named_collections>
    
    <!-- Background pool sizes -->
    <background_pool_size>16</background_pool_size>
    <background_merges_mutations_concurrency_ratio>2</background_merges_mutations_concurrency_ratio>
    <background_move_pool_size>8</background_move_pool_size>
    <background_fetches_pool_size>8</background_fetches_pool_size>
    
    <!-- ClickHouse specific settings for time-series analytics workload -->
    <merge_tree>
        <max_suspicious_broken_parts>5</max_suspicious_broken_parts>
        <max_bytes_to_merge_at_min_space_in_pool>1048576</max_bytes_to_merge_at_min_space_in_pool>
        <max_bytes_to_merge_at_max_space_in_pool>161061273600</max_bytes_to_merge_at_max_space_in_pool>
    </merge_tree>
    
    <!-- Compression for analytics data -->
    <compression>
        <case>
            <method>zstd</method>
            <level>1</level>
        </case>
    </compression>
    
    <!-- TTL settings for time-series data -->
    <merge_tree>
        <ttl_only_drop_parts>1</ttl_only_drop_parts>
    </merge_tree>

    <!-- Replication for dev: single-node embedded ClickHouse Keeper so the SAME
         Replicated* schema (periscope.sql) that runs in production also runs here.
         Embedded (in-process) Keeper is fine for a single small dev instance;
         production uses a standalone clickhouse-keeper service. -->
    <keeper_server>
        <tcp_port>9181</tcp_port>
        <server_id>1</server_id>
        <log_storage_path>/var/lib/clickhouse/coordination/log</log_storage_path>
        <snapshot_storage_path>/var/lib/clickhouse/coordination/snapshots</snapshot_storage_path>
        <coordination_settings>
            <operation_timeout_ms>10000</operation_timeout_ms>
            <session_timeout_ms>30000</session_timeout_ms>
        </coordination_settings>
        <raft_configuration>
            <server>
                <id>1</id>
                <hostname>localhost</hostname>
                <port>9234</port>
            </server>
        </raft_configuration>
    </keeper_server>

    <!-- Client config tag is <zookeeper> even though it points at Keeper. -->
    <zookeeper>
        <node>
            <host>localhost</host>
            <port>9181</port>
        </node>
    </zookeeper>

    <macros>
        <shard>01</shard>
        <replica>dev-1</replica>
    </macros>
    <default_replica_path>/clickhouse/tables/{uuid}/{shard}</default_replica_path>
    <default_replica_name>{replica}</default_replica_name>
</clickhouse>
