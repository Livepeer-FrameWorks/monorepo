{
  "uid": "player-experience",
  "title": "Player experience",
  "schemaVersion": 38,
  "timezone": "browser",
  "tags": ["frameworks", "player", "qoe", "boot", "periscope"],
  "time": { "from": "now-6h", "to": "now" },
  "templating": { "list": [] },
  "editable": true,
  "refresh": "1m",
  "panels": [
    {
      "id": 1,
      "title": "Boot volume",
      "type": "stat",
      "gridPos": { "h": 4, "w": 6, "x": 0, "y": 0 },
      "datasource": { "type": "grafana-clickhouse-datasource", "uid": "clickhouse" },
      "targets": [
        {
          "refId": "A",
          "editorType": "sql",
          "format": "table",
          "queryType": "table",
          "rawSql": "SELECT count() AS boots FROM player_boot_samples WHERE $__timeFilter(timestamp)"
        }
      ]
    },
    {
      "id": 2,
      "title": "Error + abandon rate",
      "type": "stat",
      "fieldConfig": { "defaults": { "unit": "percentunit" } },
      "gridPos": { "h": 4, "w": 6, "x": 6, "y": 0 },
      "datasource": { "type": "grafana-clickhouse-datasource", "uid": "clickhouse" },
      "targets": [
        {
          "refId": "A",
          "editorType": "sql",
          "format": "table",
          "queryType": "table",
          "rawSql": "SELECT ifNull(countIf(outcome != 'success') / nullIf(count(), 0), 0) AS not_success_rate FROM player_boot_samples WHERE $__timeFilter(timestamp)"
        }
      ]
    },
    {
      "id": 3,
      "title": "Cache hit ratio (player-owned fetches)",
      "type": "stat",
      "fieldConfig": { "defaults": { "unit": "percentunit" } },
      "gridPos": { "h": 4, "w": 6, "x": 12, "y": 0 },
      "datasource": { "type": "grafana-clickhouse-datasource", "uid": "clickhouse" },
      "targets": [
        {
          "refId": "A",
          "editorType": "sql",
          "format": "table",
          "queryType": "table",
          "rawSql": "SELECT countIf(positionCaseInsensitive(cdn_cache_status, 'hit') > 0) / nullIf(countIf(cdn_cache_status != ''), 0) AS cache_hit_ratio FROM player_boot_samples WHERE $__timeFilter(timestamp)"
        }
      ]
    },
    {
      "id": 4,
      "title": "Time-to-first-frame percentiles (ms)",
      "type": "timeseries",
      "fieldConfig": { "defaults": { "unit": "ms" } },
      "gridPos": { "h": 9, "w": 18, "x": 0, "y": 4 },
      "datasource": { "type": "grafana-clickhouse-datasource", "uid": "clickhouse" },
      "targets": [
        {
          "refId": "A",
          "editorType": "sql",
          "format": "time_series",
          "queryType": "timeseries",
          "rawSql": "SELECT $__timeInterval(timestamp) AS time, quantileIf(0.5)(total_ttf_ms, total_ttf_ms > 0) AS p50, quantileIf(0.95)(total_ttf_ms, total_ttf_ms > 0) AS p95, quantileIf(0.99)(total_ttf_ms, total_ttf_ms > 0) AS p99 FROM player_boot_samples WHERE $__timeFilter(timestamp) GROUP BY time ORDER BY time"
        }
      ]
    },
    {
      "id": 5,
      "title": "Boot span breakdown (avg ms)",
      "type": "timeseries",
      "fieldConfig": {
        "defaults": { "unit": "ms", "custom": { "stacking": { "mode": "normal" } } }
      },
      "gridPos": { "h": 9, "w": 18, "x": 0, "y": 13 },
      "datasource": { "type": "grafana-clickhouse-datasource", "uid": "clickhouse" },
      "targets": [
        {
          "refId": "A",
          "editorType": "sql",
          "format": "time_series",
          "queryType": "timeseries",
          "rawSql": "SELECT $__timeInterval(timestamp) AS time, avgIf(gateway_resolve_ms, gateway_resolve_ms > 0) AS gateway_resolve, avgIf(mist_hydrate_ms, mist_hydrate_ms > 0) AS mist_hydrate, avgIf(player_select_ms, player_select_ms > 0) AS player_select, avgIf(connect_ms, connect_ms > 0) AS connect, avgIf(prebuffer_ms, prebuffer_ms > 0) AS prebuffer FROM player_boot_samples WHERE $__timeFilter(timestamp) GROUP BY time ORDER BY time"
        }
      ]
    },
    {
      "id": 6,
      "title": "Cluster operations (token-attributed boots)",
      "type": "table",
      "gridPos": { "h": 9, "w": 24, "x": 0, "y": 22 },
      "datasource": { "type": "grafana-clickhouse-datasource", "uid": "clickhouse" },
      "targets": [
        {
          "refId": "A",
          "editorType": "sql",
          "format": "table",
          "queryType": "table",
          "rawSql": "SELECT serving_cluster_id, node_id, protocol, count() AS boots, countIf(outcome = 'error') AS errors, quantileIf(0.95)(total_ttf_ms, total_ttf_ms > 0) AS p95_ttf_ms, countIf(positionCaseInsensitive(cdn_cache_status, 'hit') > 0) / nullIf(countIf(cdn_cache_status != ''), 0) AS cache_hit_ratio FROM player_boot_samples WHERE cluster_attributed = 1 AND $__timeFilter(timestamp) GROUP BY serving_cluster_id, node_id, protocol ORDER BY boots DESC LIMIT 100"
        }
      ]
    }
  ]
}
