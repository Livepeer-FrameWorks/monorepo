---
title: GraphQL schema reference
description: Generated public GraphQL fields and types, by domain.
sidebar:
  order: 0
---

Generated from the public schema [`pkg/graphql/public/schema.public.graphql`](https://github.com/Livepeer-FrameWorks/monorepo/blob/master/pkg/graphql/public/schema.public.graphql) by `make generate-graphql-reference`. This reference covers GraphQL fields available to tenant and public clients. Operator-only and service-token-only fields, marked `@internal` in `pkg/graphql/schema.graphql`, are not part of the public schema. A field can still require a token, a role, ownership, or a product capability; read its description and the [authentication guide](/builders/api-reference/#authentication).

## Domains

| Domain | Queries | Mutations | Subscriptions | Types |
| --- | --- | --- | --- | --- |
| [Streams](/builders/api-schema/streams/) | 6 | 9 | 0 | 26 |
| [Media and storage](/builders/api-schema/media/) | 15 | 15 | 0 | 103 |
| [Access and playback](/builders/api-schema/access/) | 4 | 6 | 0 | 30 |
| [Live subscriptions](/builders/api-schema/live/) | 0 | 0 | 13 | 58 |
| [Analytics](/builders/api-schema/analytics/) | 1 | 0 | 0 | 122 |
| [Webhooks](/builders/api-schema/webhooks/) | 5 | 9 | 0 | 24 |
| [Incidents](/builders/api-schema/incidents/) | 2 | 3 | 0 | 7 |
| [Account and tenant](/builders/api-schema/account/) | 1 | 5 | 0 | 14 |
| [Billing and usage](/builders/api-schema/billing/) | 13 | 12 | 0 | 61 |
| [Clusters and infrastructure](/builders/api-schema/clusters/) | 25 | 16 | 0 | 58 |
| [Platform](/builders/api-schema/platform/) | 4 | 0 | 0 | 11 |
| [Skipper](/builders/api-schema/skipper/) | 5 | 3 | 0 | 6 |
| [Support conversations](/builders/api-schema/support/) | 3 | 2 | 0 | 8 |
| [Shared types](/builders/api-schema/shared/) | – | – | – | 69 |

## How to read this reference

- Each domain page lists its root queries, mutations, and subscriptions, then the types that only that domain's root fields reach. Types reached from two or more domains are on [Shared types](/builders/api-schema/shared/).
- A root field shows its GraphQL signature, return type, arguments with defaults, description, and deprecation. Every type name links to its definition.
- **Experimental until vX.Y.Z** marks a field that is public but not stable yet: it graduates or is removed by that platform release, and it is exempt from the schema compatibility check until then. An SDK operation whose root or selected path has such a field is marked the same way in the **SDK** table and in its SDK doc comment; an SDK release on the same line may drop it.
- **SDK** lists the generated operations that select a root field, with the TypeScript document, Go function, and Python method names. Python generates subscriptions in its async client only. A path in the **Selects** column is the field an operation below the root addresses; a type name in it stands for an inline fragment on that type (`node.<Type>.<field>` selects `node(id:) { ... on <Type> { <field> } }`).
- Every root field that is not deprecated and returns data of its own has an operation of its own. A namespace root (`Query.analytics`) returns an object whose data sits only behind argument-taking fields, at any depth, so it has no operation of its own; each of those argument fields has one, listed under the root. A deprecated root field has no generated operation; send a GraphQL document through each SDK's raw client. See the [SDK guide](/builders/sdks/).
