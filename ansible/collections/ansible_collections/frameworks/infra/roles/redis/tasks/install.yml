---
- name: Install and configure Redis via geerlingguy.redis
  ansible.builtin.include_role:
    name: geerlingguy.redis
    apply:
      tags: [install, configure, service]
  vars:
    redis_requirepass: "{{ redis_password }}"
