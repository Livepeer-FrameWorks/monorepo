---
- name: Ensure zookeeper is enabled and running
  ansible.builtin.systemd:
    name: zookeeper
    state: started
    enabled: true
    daemon_reload: "{{ (zookeeper_unit_changed is defined and zookeeper_unit_changed is changed) | bool }}"
