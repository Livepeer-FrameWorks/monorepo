---
- name: Converge
  hosts: all
  become: true
  gather_facts: true
  pre_tasks:
    - name: Skip converge when no privateer artifact pinned for the matrix
      ansible.builtin.meta: end_play
      when: privateer_artifact_url | length == 0
  tasks:
    - name: Apply frameworks.infra.privateer
      ansible.builtin.include_role:
        name: frameworks.infra.privateer
