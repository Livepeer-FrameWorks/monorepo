---
- name: mistserver | install (linux)
  ansible.builtin.import_tasks: install-linux.yml
  when: ansible_facts.os_family != 'Darwin'

- name: mistserver | install (darwin)
  ansible.builtin.import_tasks: install-darwin.yml
  when: ansible_facts.os_family == 'Darwin'
