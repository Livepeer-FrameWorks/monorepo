---
- name: Mistserver | install
  ansible.builtin.import_tasks: install.yml
  tags: [install]

- name: Mistserver | configure
  ansible.builtin.import_tasks: configure.yml
  tags: [configure]

- name: Mistserver | flush handlers before service
  ansible.builtin.meta: flush_handlers
  tags: [configure, service]

- name: Mistserver | service
  ansible.builtin.import_tasks: service.yml
  tags: [service]

- name: Mistserver | flush handlers before validate
  ansible.builtin.meta: flush_handlers
  tags: [service, validate]

- name: Mistserver | validate
  ansible.builtin.import_tasks: validate.yml
  tags: [validate]

- name: Mistserver | restart
  ansible.builtin.import_tasks: restart.yml
  tags: [restart, never]

- name: Mistserver | cleanup
  ansible.builtin.import_tasks: cleanup.yml
  tags: [cleanup, never]
