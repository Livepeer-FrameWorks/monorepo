---
extends: default

rules:
  line-length:
    max: 160
    level: warning
  braces:
    max-spaces-inside: 1
  comments:
    min-spaces-from-content: 1
  truthy:
    allowed-values: ["true", "false"]
    check-keys: false

ignore: |
  .cache/
  **/molecule/
