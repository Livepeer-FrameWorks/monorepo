---
- name: Provision Kafka MirrorMaker 2 (standalone worker)
  hosts: all
  become: true
  gather_facts: true
  gather_subset:
    - "!all"
    - min
  roles:
    - role: frameworks.infra.kafka_mirrormaker
