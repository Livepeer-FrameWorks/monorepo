// Code generated by sqlc. DO NOT EDIT.
// versions:
//   sqlc v1.31.1
// source: stale_freeze_recovery.sql

package foghorndb

import (
	"context"
)

const resetStaleFreezeAttempts = `-- name: ResetStaleFreezeAttempts :many
WITH stale AS (
    SELECT artifact_hash,
           COALESCE(sync_object_key, '')::text AS canonical_key,
           COALESCE(sync_request_id, '')::text AS attempt_id
    FROM foghorn.artifacts
    WHERE sync_status = 'in_progress'
      AND storage_location = 'freezing'
      AND status NOT IN ('deleted', 'expired', 'aborted')
      AND sync_request_id IS NOT NULL
      AND sync_node_id IS NOT NULL
      AND COALESCE(last_sync_attempt, updated_at) < NOW() - $1::bigint * INTERVAL '1 second'
    FOR UPDATE
)
UPDATE foghorn.artifacts a
SET storage_location = 'local',
    sync_status = 'failed',
    sync_error = 'sync attempt timed out; recovered for retry',
    sync_request_id = NULL,
    sync_node_id = NULL,
    updated_at = NOW()
FROM stale
WHERE a.artifact_hash = stale.artifact_hash
RETURNING stale.canonical_key, stale.attempt_id
`

type ResetStaleFreezeAttemptsRow struct {
	CanonicalKey string `db:"canonical_key" json:"canonical_key"`
	AttemptID    string `db:"attempt_id" json:"attempt_id"`
}

func (q *Queries) ResetStaleFreezeAttempts(ctx context.Context, staleSeconds int64) ([]ResetStaleFreezeAttemptsRow, error) {
	rows, err := q.db.QueryContext(ctx, resetStaleFreezeAttempts, staleSeconds)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	items := []ResetStaleFreezeAttemptsRow{}
	for rows.Next() {
		var i ResetStaleFreezeAttemptsRow
		if err := rows.Scan(&i.CanonicalKey, &i.AttemptID); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Close(); err != nil {
		return nil, err
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}
