// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.12.4
//   protoc               unknown
// source: events/public/v1/media.proto

/* eslint-disable */
import { Artifact, MediaFailureReason, mediaFailureReasonFromJSON } from "./common.js";

export interface ClipRequested {
  artifact:
    | Artifact
    | undefined;
  /** duration_ms is the requested clip length. */
  durationMs: bigint;
}

export interface ClipReady {
  artifact: Artifact | undefined;
  durationMs: bigint;
  sizeBytes: bigint;
}

export interface ClipFailed {
  artifact: Artifact | undefined;
  reason: MediaFailureReason;
}

/**
 * RecordingStarted reports that the recording node confirmed capture of the
 * stream: the first accepted progress report or media segment.
 */
export interface RecordingStarted {
  artifact: Artifact | undefined;
}

/**
 * RecordingStopped reports that capture ended and the recording is being
 * finalized. It follows recording.started once; recording.ready or
 * recording.failed follows it. A live-rewind-only recording keeps nothing
 * replayable, so recording.ready never follows it.
 */
export interface RecordingStopped {
  artifact: Artifact | undefined;
}

/**
 * RecordingReady reports that a finalized recording is saved as replayable
 * chapters. A recording whose stream keeps live rewind only never emits it.
 */
export interface RecordingReady {
  artifact: Artifact | undefined;
  durationMs: bigint;
  sizeBytes: bigint;
}

export interface RecordingFailed {
  artifact: Artifact | undefined;
  reason: MediaFailureReason;
}

export interface UploadCreated {
  artifact:
    | Artifact
    | undefined;
  /** filename is the name the tenant supplied for the upload. */
  filename: string;
  expectedSizeBytes: bigint;
}

export interface UploadCompleted {
  artifact: Artifact | undefined;
  sizeBytes: bigint;
}

export interface UploadAborted {
  artifact: Artifact | undefined;
}

export interface UploadReady {
  artifact: Artifact | undefined;
  durationMs: bigint;
  sizeBytes: bigint;
}

export interface UploadFailed {
  artifact: Artifact | undefined;
  reason: MediaFailureReason;
}

export const ClipRequested: MessageFns<ClipRequested> = {
  fromJSON(object: any): ClipRequested {
    return {
      artifact: isSet(object.artifact) ? Artifact.fromJSON(object.artifact) : undefined,
      durationMs: isSet(object.durationMs)
        ? BigInt(object.durationMs)
        : isSet(object.duration_ms)
        ? BigInt(object.duration_ms)
        : 0n,
    };
  },
};

export const ClipReady: MessageFns<ClipReady> = {
  fromJSON(object: any): ClipReady {
    return {
      artifact: isSet(object.artifact) ? Artifact.fromJSON(object.artifact) : undefined,
      durationMs: isSet(object.durationMs)
        ? BigInt(object.durationMs)
        : isSet(object.duration_ms)
        ? BigInt(object.duration_ms)
        : 0n,
      sizeBytes: isSet(object.sizeBytes)
        ? BigInt(object.sizeBytes)
        : isSet(object.size_bytes)
        ? BigInt(object.size_bytes)
        : 0n,
    };
  },
};

export const ClipFailed: MessageFns<ClipFailed> = {
  fromJSON(object: any): ClipFailed {
    return {
      artifact: isSet(object.artifact) ? Artifact.fromJSON(object.artifact) : undefined,
      reason: isSet(object.reason)
        ? mediaFailureReasonFromJSON(object.reason)
        : MediaFailureReason.MEDIA_FAILURE_REASON_UNSPECIFIED,
    };
  },
};

export const RecordingStarted: MessageFns<RecordingStarted> = {
  fromJSON(object: any): RecordingStarted {
    return { artifact: isSet(object.artifact) ? Artifact.fromJSON(object.artifact) : undefined };
  },
};

export const RecordingStopped: MessageFns<RecordingStopped> = {
  fromJSON(object: any): RecordingStopped {
    return { artifact: isSet(object.artifact) ? Artifact.fromJSON(object.artifact) : undefined };
  },
};

export const RecordingReady: MessageFns<RecordingReady> = {
  fromJSON(object: any): RecordingReady {
    return {
      artifact: isSet(object.artifact) ? Artifact.fromJSON(object.artifact) : undefined,
      durationMs: isSet(object.durationMs)
        ? BigInt(object.durationMs)
        : isSet(object.duration_ms)
        ? BigInt(object.duration_ms)
        : 0n,
      sizeBytes: isSet(object.sizeBytes)
        ? BigInt(object.sizeBytes)
        : isSet(object.size_bytes)
        ? BigInt(object.size_bytes)
        : 0n,
    };
  },
};

export const RecordingFailed: MessageFns<RecordingFailed> = {
  fromJSON(object: any): RecordingFailed {
    return {
      artifact: isSet(object.artifact) ? Artifact.fromJSON(object.artifact) : undefined,
      reason: isSet(object.reason)
        ? mediaFailureReasonFromJSON(object.reason)
        : MediaFailureReason.MEDIA_FAILURE_REASON_UNSPECIFIED,
    };
  },
};

export const UploadCreated: MessageFns<UploadCreated> = {
  fromJSON(object: any): UploadCreated {
    return {
      artifact: isSet(object.artifact) ? Artifact.fromJSON(object.artifact) : undefined,
      filename: isSet(object.filename) ? globalThis.String(object.filename) : "",
      expectedSizeBytes: isSet(object.expectedSizeBytes)
        ? BigInt(object.expectedSizeBytes)
        : isSet(object.expected_size_bytes)
        ? BigInt(object.expected_size_bytes)
        : 0n,
    };
  },
};

export const UploadCompleted: MessageFns<UploadCompleted> = {
  fromJSON(object: any): UploadCompleted {
    return {
      artifact: isSet(object.artifact) ? Artifact.fromJSON(object.artifact) : undefined,
      sizeBytes: isSet(object.sizeBytes)
        ? BigInt(object.sizeBytes)
        : isSet(object.size_bytes)
        ? BigInt(object.size_bytes)
        : 0n,
    };
  },
};

export const UploadAborted: MessageFns<UploadAborted> = {
  fromJSON(object: any): UploadAborted {
    return { artifact: isSet(object.artifact) ? Artifact.fromJSON(object.artifact) : undefined };
  },
};

export const UploadReady: MessageFns<UploadReady> = {
  fromJSON(object: any): UploadReady {
    return {
      artifact: isSet(object.artifact) ? Artifact.fromJSON(object.artifact) : undefined,
      durationMs: isSet(object.durationMs)
        ? BigInt(object.durationMs)
        : isSet(object.duration_ms)
        ? BigInt(object.duration_ms)
        : 0n,
      sizeBytes: isSet(object.sizeBytes)
        ? BigInt(object.sizeBytes)
        : isSet(object.size_bytes)
        ? BigInt(object.size_bytes)
        : 0n,
    };
  },
};

export const UploadFailed: MessageFns<UploadFailed> = {
  fromJSON(object: any): UploadFailed {
    return {
      artifact: isSet(object.artifact) ? Artifact.fromJSON(object.artifact) : undefined,
      reason: isSet(object.reason)
        ? mediaFailureReasonFromJSON(object.reason)
        : MediaFailureReason.MEDIA_FAILURE_REASON_UNSPECIFIED,
    };
  },
};

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

interface MessageFns<T> {
  fromJSON(object: any): T;
}
